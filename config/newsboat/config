auto-reload yes	
macro y set browser "setsid -f mpv %u"; open-in-browser ; set browser "elinks %u"
unbind-key h
unbind-key j
unbind-key k
unbind-key l

bind-key h quit
bind-key j next
bind-key k prev
bind-key l open
